
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Site Settings</title>

  <!-- bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <!-- custom CSS -->
  <link rel="stylesheet" href="/main.css" />
</head>

<!-- set background and text colour -->
<body class="bg-charcoal text-beige">

  <!-- container for settings form -->
  <div class="container py-5" style="max-width: 600px;">
    <h1 class="text-heading mb-4 text-center">Site Settings</h1>

    <!-- display error message -->
    <% if (error) { %>
      <div class="alert alert-danger" role="alert">
        <%= error %>
      </div>
    <% } %>

    <!-- settings form 
     POSTS to /site-settings route to update name and description
     input: 
     - site name (string)
     - description (string)
     output: form submission updates the database
     -->

    <form action="/site-settings" method="POST" class="mb-4">
      <div class="mb-3">
        <label for="siteName" class="form-label text-beige">Site Name:</label>
        <input 
          type="text" 
          id="siteName" 
          name="siteName" 
          value="<%= siteSettings.site_name %>" 
          class="form-control"
          style="background-color: #2C3639; color: #DCD7C9; border: 1px solid #566667;"
          required
        />
      </div>

      <!-- description input -->
      <div class="mb-3">
        <label for="description" class="form-label text-beige">Description:</label>
        <textarea 
          id="description" 
          name="description" 
          rows="4" 
          class="form-control"
          style="background-color: #2C3639; color: #DCD7C9; border: 1px solid #566667;"
          required
        ><%= siteSettings.description %></textarea>
      </div>

      <!-- submit button -->
      <button type="submit" class="btn btn-mocha btn-lg w-100">Update Settings</button>
    </form>

    <!-- link back to organiser home -->
    <a href="/organiser" class="btn btn-slate w-100">Back to Organiser Home Page</a>
  </div>

</body>
</html>